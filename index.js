// TODO

module.exports = {
    isPasswordCommon = function (password) {

        // Async load compiled bloom filter

        // Return promise

        // Resolve with false after timeout or error
    }
}
